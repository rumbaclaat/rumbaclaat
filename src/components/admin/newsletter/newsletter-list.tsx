// Intentionally empty. The newsletter LIST is composed in
// src/app/admin/(panel)/newsletter/page.tsx from:
//   - ExportSubscribersButton (header gold primary)  ./export-subscribers-button
//   - NewsletterGrid (AG Grid)                        ./newsletter-grid
export {};
